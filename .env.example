# Copy to .env locally (gitignored). Never commit real keys.
CURSOR_API_KEY=
RENDER_API_KEY=
WSAI_FACTORY_WEBHOOK_KEY=
# Chatbot external API gate (Vercel Production + local .env.local for API curls)
CHATBOT_API_SECRET=
